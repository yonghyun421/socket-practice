doctype html
html(lang='en')
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom 
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 Noom 
        main 
            div#welcome 
                form#name
                    input(type="text", placeholder="nickname", required)
                    button Save
                form#roomname
                    input(type="text", placeholder="room name", required)
                    button Enter Room
                h4 Open Rooms :
                ul
                div#room
                    h3 
                    //- autoplay : 자동재생
                    //- playsline : 모바일 브라우저가 필요로 하는 property
                    //- 가끔 모바일 기기로 비디오 재생 시 비디오가 전체화면이 되는데
                    //- 이를 방지해주며, 대신 웹 사이트에서만 실행된다.
                    div#myStream
                        video#myFace(autoplay,playsline,widht="400",height="400")
                        button#mute Mute
                        button#camera Turn Camera Off
                        select#cameras
                    form#message
                        input(type="text", placeholder="message", required)
                        button Send
                    ul
        
        script(Src="/socket.io/socket.io.js") 
        script(src="/public/js/app.js") 